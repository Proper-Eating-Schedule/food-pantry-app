<% if @category.errors.any? %>
    <p>Prohibited this category from being saved:</p>
    <ul>
    <% @category.errors.each do |msg| %>
      <li><%= msg.full_message %></li>
    <% end %>
    </ul>
<% end %>

<%= form_with(model: [ :admins, @category ]) do |f| %>
  <p>
    <%= f.label :name %><br/>
    <%= f.text_field :name %>
  </p>

  <p>
    <%= f.submit 'Save' %>
  </p>

<% end %>