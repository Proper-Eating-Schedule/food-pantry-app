<%= render 'admins/shared/validation_errors', record: @category %>

<%= form_with(model: [ :admins, @category ]) do |f| %>
  <p>
    <%= f.label :name %><br/>
    <%= f.text_field :name %>
  </p>

  <p>
    <%= f.submit 'Save' %>
  </p>

<% end %>